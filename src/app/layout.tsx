import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { Toaster } from "sonner";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Dwella - Dwell Better, Pay Smarter",
  description: "Verified roommate matching platform for Nigeria. Find safe, compatible roommates and affordable housing.",
  keywords: ["roommate", "housing", "Nigeria", "Lagos", "rent", "verified"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
        <Toaster position="top-center" richColors />
      </body>
    </html>
  );
}
